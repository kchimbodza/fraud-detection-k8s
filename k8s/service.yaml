apiVersion: v1
kind: Service
metadata:
  name: transaction-processor
  namespace: fintech
spec:
  selector:
    app: transaction-processor
  ports:
    - port: 80
      targetPort: 8000
  type: LoadBalancer
